# Exercice : Tests Unitaires avec pytest ✅
#
# Objectif : Tester une fonction `calculer_aire_rectangle`.
#
# 1. Écrivez la fonction `calculer_aire_rectangle(largeur, hauteur)`.
#    Si largeur ou hauteur est <= 0, elle doit lever une `ValueError`.
#
# 2. Créez un fichier de test `test_math.py` :
#    - Testez un cas normal (ex: 5, 2 -> 10).
#    - Testez un cas avec un flottant (ex: 5.5, 2 -> 11).
#    - Testez que `ValueError` est bien levée si un argument est négatif.
#      (Utilisez : with pytest.raises(ValueError):)

# VOTRE CODE ICI

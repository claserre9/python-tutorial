"""TODO : fonctions d'analyse.

- top_ips(entries, n=10) -> list[tuple[str, int]]
- status_counts(entries) -> dict[int, int]
- erreurs_5xx(entries) -> Iterable[LogEntry]  (générateur)
- volume_par_heure(entries) -> dict[int, int]  (heure 0-23 → count)
"""
